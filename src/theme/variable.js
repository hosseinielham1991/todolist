import colorPalette from './palette.js';

const variable = `
:root {
    --main-bg-color: #0074e4; /* Define a custom property --main-bg-color */
    --font-size: 16px; /* Define a custom property --font-size */
  }

`
export default variable;
